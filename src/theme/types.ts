/**
 * Theme contract
 * 
 * This is the same as the DefaultTheme from styled-components
 * @file styled.d.ts
 */
export type Theme = {
  primary: string;
  secondary: string;
  background: string;
  text: string;
}